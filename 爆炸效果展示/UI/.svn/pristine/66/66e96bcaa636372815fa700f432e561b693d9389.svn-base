<style lang="less">
	.next{
		width: 100%;
		height: 360rpx;
		padding-left: 30rpx;
		box-sizing: border-box;
		.next-title{
			height: 90rpx;
			line-height: 90rpx;
			font-size: 28rpx;
			color: #4a4a4a;
		}
		.next-list{
			padding-right: 30rpx;
			box-sizing: border-box;
			border-bottom: 2rpx solid #ececec;
			width: 100%;
			height: 90rpx;
			overflow: hidden;
			.next-name,
			.next-to,
			.next-distance,
			.next-label {
				display: inline-block;
				height: 90rpx;
				line-height: 90rpx;
			}
			.next-to,
			.next-distance,
			.next-label{
				float: right;	
				margin-left: 10rpx;			
			}
			.next-name{
				font-size: 28rpx;
				color: #333;
				float: left;
			}
			.next-to{
				image{
					width: 18rpx;
					height: 28rpx;
				}
			}
			.next-distance{
				font-size: 20rpx;
				color: #666;
			}
			.next-label{
				height: 34rpx;
				border: 2rpx solid #c2e6f7;
				border-radius: 8rpx;
				margin-top: 26rpx;
				font-size: 24rpx;
				color: #666;
				line-height: 34rpx;
				padding: 0 10rpx;
			}
		}
	}
	.comD {
		display: flex;
		flex-direction: row;
		justify-content: between;
		align-items: center;
		text-align: center;
		font-size: 28rpx;
		color: #333333;
		letter-spacing: 0.17px;
		padding: 22rpx 0 16rpx;
		line-height: 40rpx;
		border-bottom: 1px solid #ECECEC;
	}
	
	.comD .selects {
		display: inline-block;
		flex: 1;
	}
	
	.comD .selects:after {
		content: '';
		display: inline-block;
		width: 0;
		height: 0;
		border-left: 10rpx solid transparent;
		border-right: 10rpx solid transparent;
		border-top: 12rpx solid #999999;
		position: relative;
		top: -2rpx;
		right: -10rpx;
	}
	
	.comD .selectsAct {
		color: #0092FF;
		display: inline-block;
		flex: 1;
	}
	
	.comD .selectsAct:after {
		content: '';
		display: inline-block;
		width: 0;
		height: 0;
		border-left: 10rpx solid transparent;
		border-right: 10rpx solid transparent;
		border-bottom: 12rpx solid #0092FF;
		position: relative;
		top: -2rpx;
		right: -10rpx;
	}
	
	.filter-shadow {
		position: absolute;
		width: 100%;
		top: 310rpx;
		bottom: 0;
		z-index: 1;
		background: rgba(0, 0, 0, 0.5);
	}
	
	.filter-content {
		background: #ffffff;
		position: absolute;
		width: 100%;
		top: 305rpx;
		z-index: 100;
		font-size: 24rpx;
		color: #333333;
		letter-spacing: -0.14px;
		padding-top: 15rpx;
	}
	
	.filter-panel .item {
		padding: 0 30rpx;
		line-height: 70rpx;
		height: 70rpx;
		background: #f2f2f2;
		border-bottom: 1px solid #ececec;
	}
	
	.filter-panel .itemAct {
		padding: 0 30rpx;
		line-height: 70rpx;
		background: #ffffff;
		color: #0092FF;
		border-bottom: 1px solid #ececec;
	}
	
	.distanceItem {
		min-height: 360rpx;
		position: absolute;
		top: 0;
		right: 0;
		width: 40%;
		line-height: 70rpx;
		background: #ffffff;
	}
	
	.dItem {
		border-bottom: 1px solid #ececec;
		padding-left: 20rpx;
	}
	
	.dItemAct {
		border-bottom: 1px solid #ececec;
		padding-left: 20rpx;
		color: #0092FF;
	}
	
	.list{
		margin-top: 20rpx;
	}
	.scrollBody {
		.ul {
			padding-left: 30rpx;
			box-sizing: border-box;
			.recommandList {
				padding-right: 30rpx;
				border-bottom: 2rpx solid #ececec;
				box-sizing: border-box;
				.companyName {
					text-align: left;
					font-size: 28rpx;
					color: #333333;
					letter-spacing: 0.17px;
					image {
						width: 25.2rpx;
						height: 26.8rpx;
						position: relative;
						right: -14rpx;
					}
				}
				.applies {
					overflow: hidden;
					margin: 5rpx 0;
					.types,
					.list-text {
						display: inline-block;
						float: left;
						margin-right: 20rpx;
						font-size: 22rpx;
					}
					.types {
						height: 36rpx;
						width: 36rpx;
						line-height: 36rpx;
						text-align: center;
						color: #fff;
						background-color: #cce9ff;
						border-radius: 50%;
					}
					.list-text {
						height: 40rpx;
						border: 2rpx solid #c2e6f7;
						line-height: 25rpx;
						color: #999;
						border-radius: 4rpx;
						padding: 6rpx 10rpx;
						box-sizing: border-box;
					}
				}
				.otherDataService {
					font-size: 20rpx;
					display: flex;
					flex-direction: row;
					justify-content: between;
					align-items: center;
					.otherserve {
						flex: 1;
						text-align: left;
					}
					.distance {
						flex: 1;
						text-align: right;
					}
				}
				.companySite {
					font-size: 20rpx;
					text-align: left;
					padding: 14rpx 0;
				}
			}
		}
	}
	.tips {
		font-size: 28rpx;
		background: #ffffff;
		height: 60rpx;
		line-height: 60rpx;
		margin-bottom: 100rpx;
		.loadShow {
			display: flex;
			background: #ffffff;
			text-align: center;
			flex-direction: row;
			align-items: center;
			align-self: center;
			justify-content: center;
			icon {
				position: relative;
				top: 17rpx;
				right: 5rpx;
			}
		}
	}
</style>
<template>
	<view class="next">
		<!--url="-->
		<view class="next-title">下一站你还可以去</view>
		<navigator class="next-list" wx:for="{{nearInfo}}" wx:key="{{index}}" hover-class="none" url="comDetailInfo?companyId={{item.id}}">
			<view class="next-name">{{item.name}}</view>
			<view class="next-to"><image src="../resources/icons/righticon.png"></image></view>
			<view class="next-distance">{{item.distance}}</view>
			<view class="next-label">{{item.type}}</view>
		</navigator>
	</view>
	<view class="comD">
		<text bindtap="setFilterPanel" data-index="1" class="selects{{showfilterindex==1?'Act':''}}">{{selectedSite}}</text>
		<text bindtap="setFilterPanel" data-index="2" class="selects{{showfilterindex==2?'Act':''}}">{{selectedAll}}</text>
		<text bindtap="setFilterPanel" data-index="3" class="selects{{showfilterindex==3?'Act':''}}">{{selectedAllModel}}</text>
	</view>
	<view class="filter-content" wx:if="{{showfilter}}">
		<view class="filter-panel" wx:if="{{showfilterindex==1}}">
			<view class="filter-panel-left">
				<scroll-view style="min-height:360rpx;width:100%;position:relative;z-index:13;" scroll-y="true">
					<view class="distanceItem">
						<view wx:for="{{distance}}" wx:key="{{index}}" catchtap="selectDistance" data-sort="{{item}}" data-index="{{index}}" wx:if="{{nearIndex=='a'}}" class="dItem{{disIndex==index?'Act':''}}">{{item}}</view>
					</view>
					<view bindtap="selectNear" data-sort="附近" data-index="a" class="item{{nearIndex=='a'?'Act':''}}"><text>附近</text></view>
					<view wx:for="{{near}}" wx:key="{{index}}" bindtap="selectNear" data-sort="{{item.districtName}}" data-index="{{index}}" class="item{{nearIndex==index?'Act':''}}"><text></text></view>
				</scroll-view>
			</view>
		</view>
		<!--全部-->
		<view class="filter-panel" wx:if="{{showfilterindex==2}}">
			<view class="filter-panel-left">
				<scroll-view style="max-height:300px;width:100%;z-index:13;" scroll-y="true">
					<view wx:for="{{all}}" wx:key="{{index}}" bindtap="selectAll" data-sort="{{item.name}}" data-index="{{index}}" class="item{{allIndex==index?'Act':''}}">{{item.name}}</view>
				</scroll-view>
			</view>
		</view>
		<!--全部模式-->
		<view class="filter-panel" wx:if="{{showfilterindex==3}}">
			<view class="filter-panel-left">
				<scroll-view style="max-height:300px;width:100%;z-index:13;" scroll-y="true">
					<view wx:for="{{allModel}}" wx:key="{{index}}" bindtap="selectAllModel" data-sort="{{item.value}}" data-index="{{index}}" class="item{{allModelIndex==index?'Act':''}}">{{item.value}}</view>
				</scroll-view>
			</view>
		</view>
	</view>
	<view class="list">
		<scroll-view class="scrollBody" scroll-y="true" style="max-height:{{scrollWrapHeight}}px" bindscrolltolower="scrollLoading">
			<view class="ul">
				<navigator class="recommandList" wx:for="{{info}}" wx:key="{{index}}" hover-class="none" url="comDetailInfo?companyId={{item.id}}">
					<view class="companyName">
						<text>{{item.name}}</text>
						<image src="../../../resources/icons/zhu.png" wx:if="{{item.companyId > 0}}" />
					</view>
					<view class="applies">
						<view class="types" wx:for="{{item.typeArr}}" wx:for-item="i" wx:key="{{i}}">{{i}}</view>
						<view class="list-text" wx:for="{{item.baseSecondCategory}}" wx:for-item="i" wx:key="{{i}}" wx:if="{{item.baseSecondCategory != ''}}">{{i}}</view>
					</view>
					<view class="otherDataService">
						<view class="otherserve">{{item.customerGroup}}</view>
						<view class="distance">{{item.distance}}</view>
					</view>
					<view class="companySite">
						<text>{{item.provinceName}}{{item.cityName}}{{item.districtName}}{{item.address}}</text>
					</view>
				</navigator>
			</view>
			<view class="tips">
				<view wx:if="{{loadmore}}" class="loadShow">
					<icon type="waiting" size="16" /><text>玩命加载中...</text>
				</view>
				<view wx:else style="text-align:center;">
					<text>没有更多内容了</text>
				</view>
			</view>
		</scroll-view>
	</view>
	<view class="filter-shadow" wx:if="{{showfilter}}" bindtap="hideFilter"></view>
</template>
<script>
	import wepy from 'wepy'
	import { api } from '../config'
	export default class comRoundCom extends wepy.page {
		config = {
			'navigationBarTitleText': '周边企业',
			'navigationBarTextStyle': '#FFFFFF',
			'navigationBarBackgroundColor': '#0092FF'
		}
		data = {
			showfilter: false,
			showfilterindex: 0,
			selectedSite: '附近',
			selectedAll: '',
			selectedAllModel: '',
			tabItem: '原料',
			distance: [],
			near: [],
			all: [],
			allModel: [],
			nearIndex: 'a',
			allIndex: 0,
			allModelIndex: 0,
			disIndex: 0,
			currentCity: '',
			long: -1,
			lat: -1,
			cityName: '',
			pageIndex: 1,
			info: [],
			nearInfo: [],
			scrollWrapHeight: 0,
			loadmore: true,
		}
		events = {

		}
		methods = {
			scrollLoading() { //滚动加载
				if(this.loadmore) {
					this.pageIndex++;
					if(this.pageIndex<6){
						this.fetchCompanyListData();
					}else{
						this.loadmore = false;
						wx.showModal({
							title: '',
							content: '更多内容请前往按搜app',
							showCancel: false,
							confirmText: '确定',
							confirmColor: '#0092ff'
						})
					}
				}
			},
			//选项卡
			chooseType(e) {
				this.selectedSite = '附近';
				this.selectedAll = '';
				this.selectedAllModel = '';
				this.showfilter = false;
				this.showfilterindex = 0;
				this.all = e.currentTarget.dataset.a;
				this.tabItem = e.currentTarget.dataset.v;
			},
			//搜索框设置
			hideSearchIcon() {
				this.showfilter = false;
				this.showfilterindex = 0;
			},
			setFilterPanel(e) {
				this.showfilter = !this.showfilter;
				if(this.showfilter) {
					this.showfilterindex = e.currentTarget.dataset.index;
				} else {
					this.showfilterindex = 0;
				}
			},
			hideFilter() {
				this.showfilter = false;
				this.showfilterindex = 0;
			},

			selectDistance(e) {
				this.disIndex = e.currentTarget.dataset.index;
				this.selectedSite = e.currentTarget.dataset.sort;
				this.showfilter = false;
				this.showfilterindex = 0;
				this.fetchCompanyListData();
			},
			selectAll(e) {
				this.allIndex = e.currentTarget.dataset.index;
				this.selectedAll = e.currentTarget.dataset.sort;
				this.showfilter = false;
				this.showfilterindex = 0;
				this.fetchCompanyListData();
			},
			selectAllModel(e) {
				this.allModelIndex = e.currentTarget.dataset.index;
				this.selectedAllModel = e.currentTarget.dataset.sort;
				this.showfilter = false;
				this.showfilterindex = 0;
				this.fetchCompanyListData();
			}
		}
		
		onLoad(option) {
			this.cityName = option.cityName;
			this.long = option.gdlongitude;
			this.lat = option.gdlatitude;
			this.fetchCompanyFilterData();
			this.fetchCompanyListData();
			this.scrollWrapHeight = wx.getSystemInfoSync().windowHeight;
		}
		
		fetchCompanyFilterData() { //获取企业列表过滤信息                      
			let that = this;
			wepy.request({
				url: api.company.companyFilter.url,
				method: api.company.companyFilter.method,
				data: {
					cityName: that.cityName
				},
				success: function(res) {
					that.allModel = res.data.body.businessModeList;
					that.near = res.data.body.districtAreaDtoList;
					that.distance = res.data.body.nearbyList;
					that.all = res.data.body.baseFirstCategoryList[0].childs;
					that.$apply();
				}
			});
		}
		
		fetchCompanyListData() { //获取企业列表
			let that = this;
			wepy.request({
				url: api.company.companyRound.url,
				method: api.company.companyRound.method,
				data: {           
					type: that.tabItem,
					cityName: that.cityName,
					distanceCondition: that.selectedSite,
					baseThirdCategory: '', //???
					businessMode: that.selectedAllModel,
					baseSecondCategory: that.selectedAll,
					longitude: that.long,
					latitude: that.lat,
					pageInfo2: {
						pSize: 20,
						pIndex: that.pageIndex
					}
				},
				success: function(res) {
					if (res.statusCode == 200) {
						var info = that.info = res.data.body.enterpriseInfoDtoList;
						that.nearInfo = res.data.body.nearByThreeList;
						const tpages = res.data.page.tCount;
	            		const pages = info.length/20;
	            		var isInt=tpages/(that.pageIndex*20);
	            		that.loadmore = (pages==1&&isInt!=1) ? true : false;

	            		if(that.pageIndex>1){
	            			that.info = that.info.concat(info);
	            		}else{
	            			that.info = info;
	            		}
						
						
						for (var i = 0; i < info.length; i++) {
							var labels = info[i].baseSecondCategory;
		                	var label = labels.split(',');
		                	that.info[i].baseSecondCategory = label;
						}
						that.$apply();
					}
				}
			});
		}
	}
</script>